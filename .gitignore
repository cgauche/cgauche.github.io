# Build artefacts (regenerated by sync-mei.yml at every run)
.github/scripts/_site/

# Python cache
__pycache__/
*.pyc

# Notes MJ / Source backups created during refactor / OCR fix passes
*.before-template
*.before-fix
